{
  "name": "lift-workout",
  "version": "1.0.0",
  "private": true,
  "description": "LIFT — Workout generator and tracker",
  "scripts": {
    "dev": "npx serve public",
    "build": "echo 'Static site — no build step required'"
  }
}
