{
  "name": "estus-health-performance-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Estus Health Performance Lab — Workout generator and tracker",
  "scripts": {
    "dev": "npx serve public",
    "build": "echo 'Static site — no build step required'"
  }
}
